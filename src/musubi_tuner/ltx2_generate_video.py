#!/usr/bin/env python3


def main() -> None:
    raise RuntimeError(
        "This standalone generation script is not supported for official LTX-2 semantics. "
        "Use sample generation during training, or an official LTX-2 inference pipeline."
    )


if __name__ == "__main__":
    main()
